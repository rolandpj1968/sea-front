(translation-unit
  (template (params (param ? "T"))
    (func-def int "add" (params (param int "a") (param int "b"))
      (block
        (return (+ (ident "a") (ident "b"))))))
  (template (params (param ? "T") (param int "N"))
    (class-def "Array"
      (var-decl (array int 10) "data")))
  (template (params (param ? "T"))
    (class-def "Box"
      (var-decl int "val")))
  (template (params (param ? "A") (param ? "B"))
    (class-def "Pair"
      (var-decl int "first")
      (var-decl int "second")))
  (func-def int "main" (params)
    (block
      (var-decl int "x" (call (ident "add") (num 1) (num 2)))
      (var-decl struct Array "arr")
      (var-decl struct Box "nested")
      (var-decl struct Box "complex")
      (return (ident "x")))))
